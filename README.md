# Shinobu

An extensible bot framework written in Rust

### How to write a plugin

See example plugin

### How to load plugin

- Clone your plugin to `plugins` dir, then run `cargo build-plugins`
  - or move prebuilt plugin to `plugins` dir.
- Run `cargo run`
